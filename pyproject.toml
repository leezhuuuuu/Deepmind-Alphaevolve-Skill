[project]
name = "alphaevolve-skill-runtime"
version = "0.1.0"
description = "Repo-scoped AlphaEvolve-like Codex skill and local runtime MVP"
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
